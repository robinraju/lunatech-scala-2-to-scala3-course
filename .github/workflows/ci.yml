name: Scala Course CI

on:
  push:
    branches: [ exercises-master ]
  pull_request:
    branches: [ exercises-master ]

jobs:
  test_course:

    runs-on: ubuntu-latest

    steps:
      - name: Debug shell
        run: |
          ls -l /bin/sh
          dash    
          set -o | grep pipefail                                              
          set -o pipefail
          sh
          set -o pipefail
          bash --posix
          set -o pipefail
#       - name: Checkout Course Repo
#         uses: actions/checkout@v2
#         with:
#           path: lunatech-scala-2-to-scala3-course
        
#       - name: Checkout Course Management Tools
#         uses: actions/checkout@v2
#         with:
#           repository: lightbend/course-management-tools
#           path: CMT

#       - name: List workspace contents
#         run: |
#           ls -l
#           echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
#           echo "PWD: $(pwd)"
      
#       - name: Set up JDK 11
#         uses: actions/setup-java@v1
#         with:
#           java-version: 11
      
#       - name: Cache Ivy and SBT
#         uses: actions/cache@v2
#         with:
#           path: | 
#             ~/.ivy2/cache
#             ~/.sbt            
#           key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('project/**') }}
#           restore-keys: |
#             ${{ runner.os }}-sbt-ivy-cache-
#             ${{ runner.os }}-
      
#       - name: Generate Tests Script using CMT
#         run: |
#           (cd CMT/ && exec sbt "masteradm -dot -t $GITHUB_WORKSPACE/runTests $GITHUB_WORKSPACE/lunatech-scala-2-to-scala3-course")
#           cat runTests
          
#       - name: List workspace contents
#         run: |
#           ls -l
#           echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
#           echo "PWD: $(pwd)"
     
#       - name: Run Tests
#         run: |
#           chmod +x runTests
#           sh ./runTests
          
